<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_nextu_intruducion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DesarrolloApp" android:label="Desarrollo de Apps Avanzadas"></activity>
        <activity
            android:name=".ConceptoAvanzados"
            android:label="Conceptos Avanzados" />
        <activity
            android:name=".MaterialDesing"
            android:label="Material Design" />
        <activity
            android:name=".Bienvenida"
            android:label="Bienvenida">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>